// Copy this file to relay-config.js, then replace the placeholder with a long private code.
// relay-config.js is intentionally excluded from Git, so your real code stays off GitHub.
export const OVERLAY_CONTROLLER_SECRET = 'gelidiswetnow';
export const PORT = 8787; 
